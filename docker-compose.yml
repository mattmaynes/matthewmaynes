services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: mm-site
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      SITE_URL: https://matthewmaynes.com
    restart: unless-stopped
